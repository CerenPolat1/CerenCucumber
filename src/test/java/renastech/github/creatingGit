/*
git to local:
  git init
  git add .
  git commit -m "first commit"
  git branch -M main
  git remote add origin (url) https://github.com/CerenPolat1/Ceren.git
  git push -u origin main

1- sana davet istegi geliyor, bunu kabul ettikten sonra sen de o projenin bir parcasi oluyorsun
2-simdi yapman gereken github'da projeye gelip proje url'sini clone etmek
3-kendi localine gelip file-new-project version control deyip copy yaptigin url'yi yapistiriyorsun
4- simdi githuba geri don ve kendi branch'ini olustur
5-simdi lokaline gel ve sag alttan kendi branch'ini sec
6- simdi terminal'den git steplerine baslaniyor.
7- git init
   git status
   git add .
   git commit -m "commit"
   git push

*/
